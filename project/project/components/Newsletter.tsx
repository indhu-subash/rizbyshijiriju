'use client';import { FormEvent, useState } from 'react';
export function Newsletter(){const [done,setDone]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setDone(true)}return <section className="newsletter"><div className="container newsletter-inner"><div><span className="eyebrow">A little something for you</span><h2 className="serif">Stay in the loop</h2><p>Be the first to discover new drops, exclusive offers and jewellery stories.</p></div>{done?<p className="newsletter-success">You’re on the list. Welcome to Riz.</p>:<form onSubmit={submit}><label className="sr-only" htmlFor="newsletter-email">Your email address</label><input required id="newsletter-email" type="email" placeholder="Your email address"/><button className="button light">Subscribe</button></form>}</div></section>}
