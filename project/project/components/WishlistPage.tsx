'use client';import Link from 'next/link';import { Heart } from 'lucide-react';import { useStore } from './StoreProvider';import { products } from '@/data/products';import { ProductGrid } from './ProductCard';
export function WishlistPage(){const {wishlist}=useStore();const items=products.filter(p=>wishlist.includes(p.id));return <main><div className="page-intro container"><span className="eyebrow">Saved for later</span><h1 className="serif">Your wishlist</h1><p>Pieces you’re thinking about.</p></div><section className="section container">{items.length?<ProductGrid items={items}/>:<div className="empty-state"><Heart size={26} strokeWidth={1}/><h2 className="serif">Nothing saved yet.</h2><p>Tap the heart on a piece you love and find it here.</p><Link href="/shop" className="button">Explore jewellery</Link></div>}</section></main>}
